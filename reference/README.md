# Reference materials

`cursor-loop/` and `legacy-loop-workflow.md` are preserved historical materials.

They are not imported by the new application and must not be treated as its runtime implementation. The active code lives at repository root: `app/`, `src/`, `migrations/`, `scripts/` and `.project/`.
