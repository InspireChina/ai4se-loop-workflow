# LoopWork

LoopWork 以持久化事实驱动专业 Agent 持续推进软件需求，使运行过程能够跨进程退出、恢复和人工介入继续进行。

## Language

**Loop 运行**:
系统持续计算推进步骤、执行 Agent、保存结果并再次计算的一次本地运行过程。
_Avoid_: 任务运行、Agent 会话

**持续运行意图**:
用户要求系统持续维持 Loop 运行的持久化意图；它独立于任何当前进程，并且只由明确停止关闭。
_Avoid_: 自动重启开关、Electron 运行状态

**停止 Loop**:
立即结束当前 Loop 运行并关闭持续运行意图；未完成工作依靠持久化草稿和执行结果在以后重新开始时恢复。
_Avoid_: 暂停需求、更新静默

**更新静默**:
为应用更新立即停止 Loop 运行及全部后台活动、同时保留持续运行意图的状态；更新成功后可依该意图继续运行，更新失败时保持静默，直到用户明确重试更新或恢复 Loop。
_Avoid_: 停止运行、取消运行

**监督宿主**:
加载 Loop 运行生命周期 module、协调持久化状态并负责创建和清理 Runner 的本地进程；当前由 Electron 担任，未来 CLI 也可以担任。
_Avoid_: UI、Runner、Server

**监督租约**:
一份有时限的持久化许可，保证同一时刻只有一个监督宿主可以协调 Runner；租约失效后其他宿主可以接管。
_Avoid_: 数据库永久锁、运行状态

**监督代次**:
监督宿主每次取得租约时获得的单调递增 token；过期代次不能再启动或操作受管进程。
_Avoid_: PID、心跳时间

**Runner**:
在监督宿主管理下领取 Loop 工作、调用 Agent CLI 并保存执行结果的后台 worker；它不接受用户意图，也不负责决定自身是否应当存在。
_Avoid_: UI 调用入口、监督宿主
